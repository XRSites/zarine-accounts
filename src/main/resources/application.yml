spring:
    datasource:
        url: jdbc:mysql://zarine.com.br:3306/zarinebr_hive?useSSL=false
        username: zarinebr_hive
        password: Zarine@2019

    jpa:
        show-sql: true
        hibernate:
            ddl-auto: update
            naming-strategy: org.hibernate.cfg.ImprovedNamingStrategy
        properties:
            hibernate:
                dialect: org.hibernate.dialect.MySQL5InnoDBDialect

    security:
      oauth2:
        client:
          registration:
            github:
              clientId: 5e6fa0b4feded95eb8c1
              clientSecret: 4c55efd00db0247cd12f7d050453cf3fb128d84b
              redirectUriTemplate: "{baseUrl}/oauth2/callback/{registrationId}"
              scope:
                - user:email
                - read:user
            mercadolibre:
              clientId: 4545636532443454
              clientSecret: DVC92Lhm3EVh66IALxIEYQzUYwM3y9EZ
              authorizationGrantType: authorization_code
              redirectUriTemplate: "{baseUrl}/oauth2/callback/{registrationId}"
              clientAuthenticationMethod: post
              scope: read,write,offline_access
          provider:
            mercadolibre:
              authorization-uri: https://auth.mercadolivre.com.br/authorization
              token-uri: https://api.mercadolibre.com/oauth/token
              user-info-uri: https://api.mercadolibre.com/users/me
              user-info-authentication-method: form
              user-name-attribute: nickname

app:
  auth:
    tokenSecret: 926D96C90030DD58429D2751AC1BDBBC
    tokenExpirationMsec: 864000000
  oauth2:
    # After successfully authenticating with the OAuth2 Provider,
    # we'll be generating an auth token for the user and sending the token to the
    # redirectUri mentioned by the frontend client in the /oauth2/authorize request.
    # We're not using cookies because they won't work well in mobile clients.
    authorizedRedirectUris:
      - http://localhost:3000/oauth2/redirect
      - myandroidapp://oauth2/redirect
      - myiosapp://oauth2/redirect

logging:
  level: 
    root: WARN
    br.com.xrsites: TRACE
    org.springframework.security: DEBUG